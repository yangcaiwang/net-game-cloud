
package com.common.module.internal.ranklist;

import com.common.module.internal.event.AbstractEventObserver;

class RankListObserver extends AbstractEventObserver {

//	@EventSubscriber
//	private void recv(RankChangedEvent event) {
//
//		if (PropertyConfig.isDebug()) {
//			log.info("排行榜[{}]中成员[{}]名次[{},{}],{}->{}", event.rankName, event.memberId, event.modifyType, event.modified, event.modifyType == RankModifyType.UP ? event.currentRank + event.modified : event.currentRank - event.modified, event.currentRank);
//		}
//	}
}
